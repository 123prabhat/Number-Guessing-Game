# Number-Guessing-Game
Number Guessing Game is implemented in Python using the random module.This game ask the user to guess a number if user guess it correctly then he will win the game if not user can try again,if user want to quit the game user must enter a value less than or equal to zero(0).
